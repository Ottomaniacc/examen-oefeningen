{% extends 'base.html.twig' %}

{% block title %}make appointment lil bro{% endblock %}

{% block body %}
    <h1>appointment</h1>
    <div class="container" >
        <div class="row">
            <div class="col-md-6">

                {{ form_start(form) }}
                {{ form_row(form.date) }}
                {{ form_row(form.time) }}
                {{ form_row(form.subject) }}
                {{ form_row(form.problems) }}
                {{ form_row(form.discussed) }}
                {{ form_row(form.user) }}




                {{ form_end(form) }}
            </div>
        </div>
    </div>

{% endblock %}
